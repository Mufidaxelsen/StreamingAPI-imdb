const myButton = (props) => {
    return (
        <div>
            <button onClick= {() => props.panggilan3 ()} class="Button">Click Me Guys</button>
        </div>
    );
};

export default myButton;